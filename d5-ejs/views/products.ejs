<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ALL PRODUCTS</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
        integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
</head>
<h1 class="text-center text-bg-dark p-3">ALL PRODUCTS</h1>

<% if(products.length > 0){ %>
    <div class="table-responsive">
        <table class="table table-dark">
            <tr class="text-center text-bg-dark p-3"> <th>TITLE</th> <th>PRICE</th> <th>DESC</th> </tr>
            <% for (const product of products) {%>
            <tr>
                <td><%=product.title%></td>
                <td> $ <%=product.price%></td>
                <td><%=product.desc%></td>
            </tr>
            <%};%>
        </table>
    </div>
<% } else{ %>
    <h3 class="alert alert-danger">No hay productos cargados</h1>
<% } %>

<a href="/"> <button class="btn btn-danger"> UPLOAD PRODUCT </button> </a>